namespace OrderingApi.Endpoints
{ 

    // Accept pagination parameters.
    // Constructs a GetOrderQuery with these parameters
    // Retrives the data and return it in a paginated format.

    public class GetOrders
    {
    }
}
